---
const reflections = [
  {
    quote: "This place has taught me so much in such a short period of time. One phrase that stood out to me was 'See the place for what it is'. This place has taught me so much about community, the warmth, the love for one's people and culture, the abundance and the interconnectedness amongst one another.",
    name: "Amos",
    image: "/amos.jpg",
    focus: "50% 34%",
  },
  {
    quote: "You’re honouring the reciprocal relationship between nature and humans. The way everyone lives has such intentionality, in spirit, emotion, body and mind.",
    name: "Tang En",
    image: "/tangen.jpg",
    focus: "50% 28%",
  },
  {
    quote: "A simple life being contented and intentional, focused lesser on what we do, but who we are and the good impact we leave on this world.",
    name: "Heather",
    image: "/heather.jpg",
    focus: "50% 30%",
  },
  {
    quote: "I think the highlights of this trip was so unexpected but I enjoyed myself so much. Thank you for giving me the time and space to rest. I think I truly felt at peace at the trek and at Ejam. Thank you so much for the experiences, and may you always have time and space to do what you love.",
    name: "Kwan Wei",
    image: "/kwanwei.jpg",
    focus: "50% 26%",
  },
  {
    quote: "十天里体验了当地的风土明情和天气的转变。它让我体验了人生中另一个生活方式，没有华丽喧闹的城市，但它的每一座山和流水深深触动了我的心。站在大自然里，我是那么渺小。每一天在Yuksom的活动都像人生课堂。我带着满满的感恩回家。欲望和知足时能平衡的，心平静了，一切都不是问题。",
    name: "Li Yan",
    image: "/liyan.jpg",
    focus: "50% 28%",
  },
  {
    quote: "This trip, this place... It's not something I ever envisioned myself being a part of. Wouldn't even know about this if Tang En didn't intro. Suffice to say it's exactly what I needed. The peace and quiet Sikkim has given will not be easily forgotten.",
    name: "Mirza",
    image: "/mirza.jpg",
    focus: "50% 30%",
  },
  {
    quote: "Community. Realising that choosing to love with a genuine heart is something that no riches can buy.",
    name: "Bing Ming",
    image: "/bingming.jpg",
    focus: "50% 28%",
  },
  {
    quote: "Sikkim also allowed me to be myself and to rediscover what it means to be my true self. In the peace of nature away from noise, stress, people, and in the company of people I feel comfortable with, I am able to just be.",
    name: "Synth-Di",
    image: "/synthdi.jpg",
    focus: "50% 35%",
  },
  {
    quote: "Sikkim is special and you can't just bring anyone here.",
    name: "Ying",
    image: "/ying.jpg",
    focus: "50% 35%",
  },
  {
    quote: "This has been an excellent getaway for me in all aspects. Thank you for reinviting me with our community partners in Gangtok and Yuksam, and introducing me to new communities at Dzongu.",
    name: "Vilma",
    image: "/vilma.jpg",
    focus: "50% 35%",
  },
  {
    quote: "I am so glad that we met serendipitously and I actually managed to live my dreams of travelling to this Shangrila.",
    name: "Satya",
    image: "/satya.jpg",
    focus: "50% 35%",
  },
  {
    quote: "Thanks for organising such a beautiful trip. Your love for Sikkim shines through in everything you do, and looking forward to the good you will do for the community.",
    name: "JX",
    image: "/jx.jpg",
    focus: "50% 35%",
  },
  {
    quote: "The experience in Sikkim is so precious and truly invaluable. I've learnt so much more about the indigenous people, the various challenges faced by the people here, their way of life and their love for their culture and land.",
    name: "Sharon",
    image: "/sharon.jpg",
    focus: "50% 35%",
  },
  {
    quote: "You're doing a wonderful job in showcasing the community and scenery in a way that is palatable for both the community and the travellers.",
    name: "Kellie",
    image: "/kellie.jpg",
    focus: "50% 35%",
  },
  {
    quote: "希望Feels Like Om越做越长久，让Sikkim这个美好的地方能够吸引到它的有缘人。",
    name: "Lianne",
    image: "/lianne.jpg",
    focus: "50% 35%",
  },
  {
    quote: "I'm going to miss this place so much and waking up to hearing Hachi being scolded by you HAHA.",
    name: "Zi Qing",
    image: "/ziqing.jpg",
    focus: "50% 35%",
  },
  {
    quote: "I liked that the people here are always thinking about giving back to the community.",
    name: "Samantha",
    image: "/samantha.jpg",
    focus: "50% 35%",
  },
  {
    quote: "Knowing you're exactly where you need to be in the moment. Feeling free to roam and return whenever the time is right. To trust that things will unfold in their time. And most of all, to love and know yourself, be loved in all the places you will call home.",
    name: "Cheryl",
    image: "/cheryl.jpg",
    focus: "50% 35%",
  },
  {
    quote: "This trip is so special in many ways. It's special to travel and be included within the community, to see life through their eyes, a lifestyle, anchored on abundance yet live sustainably; where life and nature are one, us and others live supporting one another, where integrity and relationship means much more than $",
    name: "Angela",
    image: "/angela.jpg",
    focus: "50% 35%",
  },
  {
    quote: "You care deeply, for the families here, for their stories, for preserving their way of life, and for helping visitors experience Sikkim in a way that is respectful, grounding and real. You've created something that goes far beyond a retreat -- a bridge, one built on sincerity, patience and quiet strength.",
    name: "Marilyn",
    image: "/marilyn.jpg",
    focus: "50% 35%",
  },
  {
    quote: "We hear your struggles amidst your passion in creating a community that is vibrant like Sikkim -- we are a testament to your efforts and a reminder that your great work has impacted us.",
    name: "Justin",
    image: "/justin.jpg",
    focus: "50% 35%",
  },
  {
    quote: "Everyone has been so kind and generous that it inspires me to be a better person like the lama said do good from a place of pure heart and soul because we are all dying. When we pass over to the next life, we are bringing nothing with us. This was truly once in a lifetime kind of adventure, I'm glad I signed up and will recommend Feels Like Om to my friends.",
    name: "Hilda",
    image: "/hilda.jpg",
    focus: "50% 35%",
  },
  {
    quote: "Thank you for showing me the real Sikkim, its beauty, its calmness and the warmth of its people. In the course of days, this place has come to feel like home and family to me.",
    name: "Kai Xuan",
    image: "/kaixuan.jpg",
    focus: "50% 35%",
  },
  {
    quote: "Thank you for enabling my discovery of Sikkim! Have treasured the experience and the grace that joined out paths briefly in this time. There are no coincidences -- you are here for reasons that the universe has and will continue to reveal to you in its time and infinite wisdom. Go where your inner consciousness guides you.",
    name: "Nitya",
    image: "/nitya.jpg",
    focus: "50% 35%",
  },
  {
    quote: "Yong Xi shared a quote: ‘You are forever becoming. There is no landing place, no arrival. This will scare you sometimes, but learning to swim in the waters of the liminal is where you'll come the most vibrantly alive.’ - Lisa Oliveria",
    name: "Yong Xi",
    image: "/yongxi.jpg",
    focus: "50% 35%",
  },
  {
    quote: `Through our long, heart-to-heart chats, I began to understand the courage it took Zi Hui to walk away from a typical trajectory. 'This journey from career burnout in Singapore to living in alignment taught me that life doesn't need to follow the well-trodden path,' she told me. 'Living simply and close to nature in Sikkim clarified what truly matters to me: connection, relationships, and love expressed through giving back to the community.' From her, I learned about resilience and staying open when life veers away from what we imagined. Her fearlessness, her boundless curiosity, her instinctive service heart stayed with me. She left me with a lot of wisdom.`,
    name: "Ee Ming",
    image: "/eeming.jpg",
    focus: "50% 35%",
  },
];

const baseWidth = 1180;
const baseHeight = 900;
const extraRings = Math.max(0, Math.ceil((reflections.length - 18) / 12));
const webWidth = baseWidth + extraRings * 260;
const webHeight = baseHeight + extraRings * 200;
const centre = { x: webWidth / 2, y: webHeight / 2 };

const reflectionNodes = reflections.map((reflection, index) => {
  const ring = index < 6 ? 0 : 1 + Math.floor((index - 6) / 12);
  const ringStart = ring === 0 ? 0 : 6 + (ring - 1) * 12;
  const remaining = reflections.length - ringStart;
  const ringCount = Math.min(ring === 0 ? 6 : 12, remaining);
  const position = index - ringStart;
  const angleOffset = ring % 2 === 0 ? -Math.PI / 2 : -Math.PI / 2 + 0.12;
  const angle = angleOffset + (position / ringCount) * Math.PI * 2;
  const radiusX = 250 + ring * 160;
  const radiusY = 190 + ring * 140;
  const wobble = ((index % 5) - 2) * 7;

  return {
    ...reflection,
    ring,
    position,
    ringCount,
    x: centre.x + Math.cos(angle) * (radiusX + wobble),
    y: centre.y + Math.sin(angle) * (radiusY - wobble * 0.45),
    size: 62 + (index % 4) * 7,
  };
});

const curvePath = (a, b, bend = 0.1) => {
  const mx = (a.x + b.x) / 2;
  const my = (a.y + b.y) / 2;
  const dx = b.x - a.x;
  const dy = b.y - a.y;
  const length = Math.max(1, Math.hypot(dx, dy));
  const offset = length * bend;
  const cx = mx - (dy / length) * offset;
  const cy = my + (dx / length) * offset;
  return `M ${a.x.toFixed(1)} ${a.y.toFixed(1)} Q ${cx.toFixed(1)} ${cy.toFixed(1)} ${b.x.toFixed(1)} ${b.y.toFixed(1)}`;
};

const webPaths = [];
const rings = Array.from({ length: extraRings + 2 }, (_, ringIndex) => reflectionNodes.filter((node) => node.ring === ringIndex)).filter((ring) => ring.length);

rings.forEach((ringNodes, ringIndex) => {
  ringNodes.forEach((node, index) => {
    if (ringNodes.length > 1) {
      const next = ringNodes[(index + 1) % ringNodes.length];
      webPaths.push(curvePath(node, next, index % 2 ? 0.07 : -0.07));
    }
    if (ringNodes.length > 5) {
      const skip = ringNodes[(index + 2) % ringNodes.length];
      webPaths.push(curvePath(node, skip, index % 3 === 0 ? 0.11 : -0.08));
    }
    if (ringIndex > 0 && rings[ringIndex - 1]?.length) {
      const innerRing = rings[ringIndex - 1];
      const inner = innerRing[Math.round((index / ringNodes.length) * innerRing.length) % innerRing.length];
      webPaths.push(curvePath(node, inner, index % 2 ? 0.08 : -0.09));
    }
    if (index % 4 === 0) {
      webPaths.push(curvePath(node, centre, index % 2 ? 0.08 : -0.08));
    }
  });
});
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Reflections | Feels Like Om</title>
    <link rel="stylesheet" href="/type-system.css" />
  </head>
  <body>
    <header class="site-header">
      <a class="brand" href="/">Feels Like Om</a>
      <button class="mobile-nav-toggle" type="button" aria-label="Open navigation" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
      <nav class="main-nav" aria-label="Main navigation">
        <details class="nav-dropdown">
          <summary>ABOUT</summary>
          <div class="submenu">
            <a href="/about/our-stories">Our stories</a>
            <a href="/about/getting-to-sikkim">Getting to Sikkim</a>
            <a href="/about/visa-and-permits">Visa and permits</a>
            <a href="/about/life-on-sikkim-time">Life on Sikkim time</a>
          </div>
        </details>
        <details class="nav-dropdown">
          <summary>JOURNEYS</summary>
          <div class="submenu">
            <a href="/journeys/the-flagship">The Flagship</a>
            <a href="/journeys/how-to-embrace-suffering">To Be Free</a>
          </div>
        </details>
        <a href="/reflections" class="active" aria-current="page">REFLECTIONS</a>
        <a href="/contact">CONTACT</a>
      </nav>
    </header>

    <main>
      <section class="reflection-intro">
        <p class="eyebrow">REFLECTIONS</p>
        <h1>We are all connected.</h1>
        <p class="intro">Each circle holds one person’s reflection. Follow the threads, click into a story, and move from one reflection to the next.</p>
      </section>

      <section class="reflection-web-section" aria-label="Interconnected participant reflections">
        <div class="reflection-web-scroller">
          <div class="reflection-web" style={`--web-width:${webWidth}px;--web-height:${webHeight}px;`}>
            <svg class="web-lines" viewBox={`0 0 ${webWidth} ${webHeight}`} aria-hidden="true">
              <g class="web-ring">
                {webPaths.map((path) => <path d={path} />)}
              </g>
            </svg>

            <div class="flo-center" aria-label="Feels Like Om">
              <img src="/Logo-Circle.png" alt="Feels Like Om circular logo" />
            </div>

            {reflectionNodes.map((reflection, index) => (
              <button
                class={`reflection-node${reflection.image ? " has-image" : ""}`}
                type="button"
                style={`--x:${reflection.x}px;--y:${reflection.y}px;--node-size:${reflection.size}px;--image:${reflection.image ? `url('${reflection.image}')` : "none"};--focus:${reflection.focus};`}
                data-index={index}
                data-quote={reflection.quote}
                data-name={reflection.name}
                data-image={reflection.image}
                data-focus={reflection.focus}
                aria-label={`Open reflection from ${reflection.name}`}
              >
                <span aria-hidden="true"></span>
              </button>
            ))}
          </div>
        </div>
      </section>
    </main>

    <dialog class="reflection-lightbox" aria-label="Participant reflection" tabindex="-1">
      <button class="lightbox-close" type="button" aria-label="Close reflection">×</button>
      <button class="lightbox-nav lightbox-prev" type="button" aria-label="Previous reflection">‹</button>
      <button class="lightbox-nav lightbox-next" type="button" aria-label="Next reflection">›</button>
      <div class="lightbox-copy">
        <blockquote class="lightbox-quote" data-length="short">“Reflection coming soon.”</blockquote>
        <p class="lightbox-name">— Past participant</p>
        <a
          class="lightbox-full-reflection"
          href="https://softearthdreams.substack.com/p/soul-lessons-from-a-mountain-dog"
          target="_blank"
          rel="noreferrer"
          hidden
        >Read the full reflection here</a>
      </div>
    </dialog>

    <script>
      const nodes = Array.from(document.querySelectorAll('.reflection-node'));
      const dialog = document.querySelector('.reflection-lightbox');
      const quote = dialog?.querySelector('.lightbox-quote');
      const name = dialog?.querySelector('.lightbox-name');
      const fullReflectionLink = dialog?.querySelector('.lightbox-full-reflection');
      const close = dialog?.querySelector('.lightbox-close');
      const prev = dialog?.querySelector('.lightbox-prev');
      const next = dialog?.querySelector('.lightbox-next');
      const webScroller = document.querySelector('.reflection-web-scroller');
      let current = 0;
      let savedScrollX = 0;
      let savedScrollY = 0;
      let savedWebScrollLeft = 0;

      const quoteLengthTier = (text) => {
        const length = text.trim().length;
        if (length <= 90) return 'short';
        if (length <= 150) return 'medium';
        if (length <= 220) return 'long';
        if (length <= 300) return 'very-long';
        return 'extra-long';
      };

      const showReflection = (index) => {
        if (!nodes.length || !dialog) return;
        current = (index + nodes.length) % nodes.length;
        const node = nodes[current];
        const quoteText = node.dataset.quote ?? '';
        if (quote) {
          quote.textContent = `“${quoteText}”`;
          quote.dataset.length = quoteLengthTier(quoteText);
        }
        if (name) name.textContent = `— ${node.dataset.name ?? ''}`;
        if (fullReflectionLink) {
          fullReflectionLink.hidden = node.dataset.name !== 'Ee Ming';
        }
        if (!dialog.open) {
          savedScrollX = window.scrollX;
          savedScrollY = window.scrollY;
          savedWebScrollLeft = webScroller?.scrollLeft ?? 0;
          dialog.showModal();
          document.documentElement.classList.add('reflection-modal-open');
        }
        dialog.scrollTop = 0;
        requestAnimationFrame(() => {
          dialog.scrollTop = 0;
          dialog.focus({ preventScroll: true });
        });
      };

      const restoreReflectionPosition = () => {
        requestAnimationFrame(() => {
          nodes[current]?.focus({ preventScroll: true });
          window.scrollTo(savedScrollX, savedScrollY);
          if (webScroller) webScroller.scrollLeft = savedWebScrollLeft;
        });
      };

      nodes.forEach((node, index) => node.addEventListener('click', () => showReflection(index)));
      close?.addEventListener('click', () => dialog?.close());
      prev?.addEventListener('click', () => showReflection(current - 1));
      next?.addEventListener('click', () => showReflection(current + 1));
      dialog?.addEventListener('click', (event) => {
        if (event.target === dialog) dialog.close();
      });
      dialog?.addEventListener('close', () => {
        document.documentElement.classList.remove('reflection-modal-open');
        restoreReflectionPosition();
      });
      document.addEventListener('keydown', (event) => {
        if (!dialog?.open) return;
        if (event.key === 'ArrowLeft') showReflection(current - 1);
        if (event.key === 'ArrowRight') showReflection(current + 1);
      });
    </script>
      <script src="/mobile-nav.js" is:inline></script>
  </body>
</html>

<style>
  :global(*) { box-sizing:border-box; }
  :global(html), body { margin:0; background:#f4efe5; color:#18301f; overflow-x:hidden; }
  :global(html.reflection-modal-open), :global(html.reflection-modal-open body) { overflow:hidden!important; }

  .site-header { display:flex; align-items:center; justify-content:space-between; gap:2rem; padding:1.5rem 4%; border-bottom:1px solid rgba(24,48,31,.15); }
  .brand { color:inherit; text-decoration:none; white-space:nowrap; }
  .main-nav { display:flex; align-items:center; gap:1.5rem; letter-spacing:.08em; }
  .main-nav>a,.main-nav summary { color:inherit; text-decoration:none; cursor:pointer; }
  .main-nav .active { border-bottom:1px solid currentColor; }
  .nav-dropdown { position:relative; }
  .nav-dropdown summary { list-style:none; }
  .nav-dropdown summary::-webkit-details-marker { display:none; }
  .submenu { position:absolute; top:calc(100% + .65rem); right:0; width:max-content; min-width:13rem; padding:.7rem; background:#fffaf1; border-radius:.4rem; box-shadow:0 12px 30px rgba(8,20,12,.13); z-index:8; opacity:0; visibility:hidden; transform:translateY(-4px); transition:opacity 160ms ease,visibility 160ms ease,transform 160ms ease; }
  .submenu::before { content:""; position:absolute; top:-.75rem; left:0; right:0; height:.75rem; }
  .submenu a { display:block; padding:.65rem .75rem; color:#18301f; text-decoration:none; letter-spacing:0; }
  .submenu a:hover { background:rgba(24,48,31,.06); }
  @media (hover:hover) and (pointer:fine) { .nav-dropdown:hover .submenu { opacity:1; visibility:visible; transform:translateY(0); } }

  main { width:100%; }
  .reflection-intro { width:min(1180px,calc(100% - 8%)); margin:0 auto; padding:clamp(5rem,10vw,8rem) 0 1rem; text-align:center; }
  .eyebrow { margin:0 0 1.2rem; letter-spacing:.14em; }
  .reflection-intro h1 { margin:0; font-size:clamp(3rem,6vw,6.5rem)!important; line-height:.98; }
  .intro { width:min(620px,100%); margin:1.5rem auto 0; line-height:1.7; }

  .reflection-web-section { width:100%; padding:clamp(2rem,5vw,4rem) 0 clamp(5rem,9vw,8rem); overflow:hidden; }
  .reflection-web-scroller { width:100%; overflow-x:auto; overflow-y:hidden; scrollbar-width:thin; scrollbar-color:rgba(24,48,31,.24) transparent; }
  .reflection-web { position:relative; width:var(--web-width); height:var(--web-height); margin:0 auto; }
  .web-lines { position:absolute; inset:0; width:100%; height:100%; overflow:visible; pointer-events:none; }
  .web-ring { fill:none; stroke:#18301f; stroke-width:1.15; opacity:.34; stroke-linecap:round; stroke-linejoin:round; }

  .flo-center { position:absolute; left:50%; top:50%; width:clamp(120px,17vw,210px); aspect-ratio:1; transform:translate(-50%,-50%); border-radius:50%; overflow:hidden; background:transparent; border:0; box-shadow:none; z-index:3; }
  .flo-center img { display:block; width:100%; height:100%; object-fit:contain; border-radius:50%; }

  .reflection-node { position:absolute; left:var(--x); top:var(--y); width:var(--node-size); aspect-ratio:1; transform:translate(-50%,-50%); border-radius:50%; border:2px solid #18301f; padding:0; cursor:zoom-in; background:#f4efe5; z-index:4; overflow:hidden; transition:transform 180ms ease,box-shadow 180ms ease; }
  .reflection-node span { display:block; width:100%; height:100%; border-radius:50%; background:linear-gradient(145deg,rgba(24,48,31,.24),rgba(24,48,31,.06)); }
  .reflection-node.has-image span { background-image:var(--image); background-size:cover; background-position:var(--focus); background-repeat:no-repeat; }
  .reflection-node:hover,.reflection-node:focus-visible { transform:translate(-50%,-50%) scale(1.12); box-shadow:0 10px 25px rgba(24,48,31,.16); outline:none; }

  .reflection-lightbox { position:fixed; inset:0; width:min(92vw,940px); max-width:940px; max-height:92vh; margin:auto; padding:clamp(2.5rem,5vw,4rem) clamp(4.5rem,8vw,7rem); border:0; background:#f4efe5; color:#18301f; box-shadow:0 24px 70px rgba(7,18,11,.34); overflow:auto; overscroll-behavior:contain; }
  .reflection-lightbox::backdrop { background:rgba(7,18,11,.78); backdrop-filter:blur(4px); }
  .lightbox-copy { text-align:center; margin:0 auto; max-width:760px; }
  .lightbox-quote { margin:0 0 .8rem; font-family:Georgia,'Times New Roman',serif!important; line-height:1.13; text-wrap:balance; overflow-wrap:anywhere; }
  .lightbox-quote[data-length="short"] { font-size:clamp(2rem,3vw,2.6rem)!important; }
  .lightbox-quote[data-length="medium"] { font-size:clamp(1.75rem,2.55vw,2.25rem)!important; }
  .lightbox-quote[data-length="long"] { font-size:clamp(1.5rem,2.2vw,1.95rem)!important; line-height:1.15; }
  .lightbox-quote[data-length="very-long"] { font-size:clamp(1.3rem,1.85vw,1.65rem)!important; line-height:1.18; }
  .lightbox-quote[data-length="extra-long"] { font-size:clamp(1.15rem,1.55vw,1.4rem)!important; line-height:1.22; }
  .lightbox-name { margin:0; opacity:.72; }
  .lightbox-full-reflection { display:inline-block; margin-top:1.1rem; color:inherit; font-family:var(--font-body); font-size:.95rem; text-underline-offset:.3rem; }
  .lightbox-full-reflection[hidden] { display:none; }
  .lightbox-close { position:sticky; top:0; float:right; width:2.7rem; height:2.7rem; margin:0 -4rem -2.7rem auto; border:0; border-radius:50%; background:rgba(255,250,241,.96); color:#18301f; font-size:1.8rem; cursor:pointer; z-index:6; }
  .lightbox-nav { position:absolute; top:50%; width:3.2rem; height:3.2rem; transform:translateY(-50%); border:1px solid rgba(24,48,31,.18); border-radius:50%; background:rgba(244,239,229,.96); color:#18301f; font-family:Georgia,serif; font-size:2.1rem; cursor:pointer; z-index:6; box-shadow:0 8px 24px rgba(7,18,11,.16); }
  .lightbox-prev { left:1.25rem; }
  .lightbox-next { right:1.25rem; }

  @media (max-width:760px) {
    .site-header { align-items:center; padding:1.15rem 1.25rem; min-height:72px; }
    .main-nav { position:fixed; top:1rem; right:1rem; width:48px; height:48px; z-index:100; display:block; overflow:visible; }
    .main-nav > * { display:none; }
    .main-nav > .nav-dropdown:first-child { display:block; position:static; }
    .main-nav > .nav-dropdown:first-child > summary { position:absolute; top:0; right:0; display:flex; width:48px; height:48px; align-items:center; justify-content:center; border:1px solid rgba(24,48,31,.16); border-radius:50%; background:rgba(244,239,229,.96); font-size:0; }
    .main-nav > .nav-dropdown:first-child > summary::before { content:""; width:22px; height:2px; background:#18301f; box-shadow:0 -7px 0 #18301f,0 7px 0 #18301f; }
    .main-nav:has(> .nav-dropdown:first-child[open]) { width:min(88vw,360px); height:auto; max-height:calc(100dvh - 2rem); padding:4.4rem 1rem 1rem; overflow-y:auto; background:#f4efe5; border:1px solid rgba(24,48,31,.12); border-radius:.65rem; box-shadow:0 18px 48px rgba(8,20,12,.22); }
    .main-nav:has(> .nav-dropdown:first-child[open]) > * { display:block; width:100%; }
    .main-nav:has(> .nav-dropdown:first-child[open]) > .nav-dropdown:first-child > summary { top:.7rem; right:.7rem; }
    .main-nav:has(> .nav-dropdown:first-child[open]) > .nav-dropdown:not(:first-child) > summary,
    .main-nav:has(> .nav-dropdown:first-child[open]) > a { display:block; width:100%; padding:1rem .5rem; border-top:1px solid rgba(24,48,31,.12); font-size:.78rem!important; letter-spacing:.1em; }
    .main-nav:has(> .nav-dropdown:first-child[open]) .submenu { position:static; width:100%; min-width:0; padding:.2rem 0 .7rem; background:transparent; box-shadow:none; transform:none; }
    .main-nav:has(> .nav-dropdown:first-child[open]) .nav-dropdown[open] > .submenu { opacity:1; visibility:visible; display:block; }
    .main-nav:has(> .nav-dropdown:first-child[open]) .nav-dropdown:not([open]) > .submenu { display:none; }
    .main-nav:has(> .nav-dropdown:first-child[open]) .submenu a { display:block; padding:.8rem .8rem; white-space:normal; }
    .reflection-intro { width:calc(100% - 2.5rem); padding-top:4rem; }
    .reflection-web-scroller { padding-bottom:.5rem; }
    .reflection-web { margin-left:0; }
    .reflection-node { width:max(58px,calc(var(--node-size) * .86)); }
    .flo-center { width:150px; }
    .reflection-lightbox { width:calc(100vw - 1rem); max-width:calc(100vw - 1rem); max-height:calc(100dvh - 1rem); inset:.5rem; margin:auto; padding:3.6rem 3.2rem 3rem; }
    .lightbox-copy { max-width:100%; }
    .lightbox-quote[data-length="short"] { font-size:clamp(1.55rem,6vw,1.9rem)!important; }
    .lightbox-quote[data-length="medium"] { font-size:clamp(1.4rem,5.3vw,1.68rem)!important; }
    .lightbox-quote[data-length="long"] { font-size:clamp(1.25rem,4.7vw,1.5rem)!important; }
    .lightbox-quote[data-length="very-long"] { font-size:clamp(1.1rem,4.15vw,1.3rem)!important; line-height:1.2; }
    .lightbox-quote[data-length="extra-long"] { font-size:clamp(1rem,3.8vw,1.16rem)!important; line-height:1.24; }
    .lightbox-nav { width:2.7rem; height:2.7rem; font-size:1.7rem; }
    .lightbox-prev { left:.45rem; }
    .lightbox-next { right:.45rem; }
    .lightbox-close { position:absolute; top:.7rem; right:.7rem; margin:0; }
  }
</style>
